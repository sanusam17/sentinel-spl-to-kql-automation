You are a Microsoft Sentinel Content Developer.

Convert the following KQL query into a VALID Azure Sentinel Scheduled Analytics Rule ARM template.

STRICT OUTPUT RULES:
- Return ONLY valid JSON (no markdown, no explanation)
- Must be deployable in Azure Sentinel

RESOURCE TYPE:
Microsoft.OperationalInsights/workspaces/providers/alertRules

REQUIRED ARM STRUCTURE:
- name
- type
- apiVersion: 2023-02-01-preview
- properties object

INSIDE properties include:
- displayName (derive from KQL intent)
- description (short detection purpose)
- severity (Low, Medium, High, Informational)
- enabled: true
- query (exact KQL, unchanged)
- queryFrequency (PT5M default unless logic suggests otherwise)
- queryPeriod (PT5M default unless logic suggests otherwise)
- triggerOperator (GreaterThan recommended unless logic suggests otherwise)
- triggerThreshold (default 0 unless logic suggests otherwise)
- tactics (array of MITRE ATT&CK tactics if inferable else empty array)

RULES:
- Do NOT hallucinate security context not present in KQL
- Keep query intact
- Ensure valid JSON

KQL:
{KQL_QUERY}